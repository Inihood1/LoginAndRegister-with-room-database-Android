<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.inihood.loginandregistermvvm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:screenOrientation="portrait"
            android:theme="@style/LoginSignUpTheme"
            android:windowSoftInputMode="adjustResize"
            android:name=".auth.auth.LoginActivity">

        </activity>

        <activity
            android:screenOrientation="portrait"
            android:theme="@style/LoginSignUpTheme"
            android:windowSoftInputMode="adjustResize"
            android:name=".NoteListActivity" />

        <activity
            android:name=".auth.auth.RegisterActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/LoginSignUpTheme"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddNoteActivity"
            android:label="@string/title_activity_add_note" />
        <activity
            android:name=".auth.DashboardActivity"
            android:screenOrientation="portrait"
            android:theme="@style/MenuNavigation6Theme" />

    </application>

</manifest>